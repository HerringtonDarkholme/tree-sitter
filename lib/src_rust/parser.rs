#![allow(dead_code)]

// Stub for parser.c — The LR parsing engine.
// This is the core of tree-sitter: shift/reduce parsing with
// error recovery, GLR support, and incremental reparsing.
//
// Must be the last core file rewritten (Tier 4) since it depends
// on ALL other modules.
